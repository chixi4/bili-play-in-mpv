# Changelog

## 2026-02-18

### Added

- 启动器增加 YouTube 域名识别，支持 `youtube.com`、`m.youtube.com`、`youtu.be`。
- 启动器增加 `11` 位 YouTube 视频 ID 直开能力。
- 安装脚本和文档补充 YouTube 命令行与协议示例。

### Changed

- 协议 URL 的二次解码白名单扩展到 YouTube 场景，减少书签和协议链路失败。
- 书签脚本从单一 `iframe` 触发改为多通道触发，依次尝试链接点击、`iframe` 和临时新标签，提升浏览器兼容性。
- 书签脚本改为精简版本，固定使用隐藏链接点击与新标签协议唤起，避免当前标签页跳白并降低复制出错率。
- 书签进一步缩短为 `window.open` 主路径，失败时再回退链接点击，降低手工粘贴出错概率。
- 书签回退逻辑已移除，固定为单一路径 `window.open`。
- 书签触发路径改回单一路径隐藏 `iframe`，避免弹出 `mpvplay://` 空白标签页。

## 2026-02-17

### Added

- 初始化项目目录，归档当前可用脚本和配置。
- 增加 `install.ps1` 和 `uninstall.ps1`。
- 增加 `mpvplay://` 协议注册与恢复逻辑。
- 增加书签后台触发脚本模板，避免当前页面白屏。
- 增加快速开始与排障文档。

### Changed

- 同步仓库脚本到当前实测版本，更新 `src/mpv-scripts/bili_live_danmaku.lua`。
- 同步默认配置为当前状态，新增 `opacity`、`simple_spawn_mode`、`video_prefill_enabled`，默认字号改为 `72`。
- 字号运行时调节上限从 `72` 扩展到 `144`。
- 默认关闭点播预填充，保持实时时间轴发射策略。
- 修正项目名称拼写为 `bili-play`，并更新安装清单文件名。
- 卸载脚本增加旧清单名兼容读取。
- 更新 README 与架构、排障文档，使说明与当前默认行为一致。

### Notes

- 本版本以现网可用配置为基线，重点保留弹幕滚动优化结果。
